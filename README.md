# SpamWav1
```
$ apt update && apt upgrade -y
$ apt install git
$ apt install python
$ python3 -m pip install requests
$ git clone https://github.com/benicuy/Spamwav1
$ cd SpamWav1
$ pyhton3 spam.py
```
